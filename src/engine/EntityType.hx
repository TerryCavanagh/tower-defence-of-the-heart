package engine;

enum EntityType{
  ENEMY; //All enemies are exactly the same: variables are changed in the waves
  
  TOWER_SHOOTY;
  TOWER_BEAM;
  TOWER_VORTEX;
  
  BULLET;
  BEAM;
  VORTEX;
  
  GOAL;
}